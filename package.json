{
  "name": "igc-web",
  "private": true,
  "type": "module",
  "description": "In Great Company — static site on Cloudflare Pages with D1-backed Functions. No build step: the browser gets the files in this repo as-is. This manifest exists only to pin the wrangler CLI so local dev and database commands behave the same on every machine.",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "db:schema:local": "wrangler d1 execute igc --local --file=./db/schema.sql",
    "db:schema:remote": "wrangler d1 execute igc --remote --file=./db/schema.sql",
    "db:seed:local": "wrangler d1 execute igc --local --file=./db/seed.sql",
    "db:seed:remote": "wrangler d1 execute igc --remote --file=./db/seed.sql"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
